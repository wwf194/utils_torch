# ~/.bashrc: executed by bash(1) for non-login shells.
# see /usr/share/doc/bash/examples/startup-files (in the package bash-doc)
# for examples

# If not running interactively, don't do anything

#eval 'ssh-agent -s'
#eval 'ssh-agent bash'
#exec ssh-agent bash # this works, but .bashrc ends up in infinite recursion and has to be stopped by Ctrl + C.
if [[ "$SSH_AUTH_SOCK" = "" ]]; then
    # on the first round, we do this...
    exec ssh-agent bash
else
    # ... and when ssh-agent is running, we do this instead.
    ssh-add /data4/wangweifan/.ssh/id_rsa_github
fi

case $- in
    *i*) ;;
      *) return;;
esac

# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
__conda_setup="$('/home/wangweifan/data/anaconda3/bin/conda' 'shell.bash' 'hook' 2> /dev/null)"
if [ $? -eq 0 ]; then
    eval "$__conda_setup"
else
    if [ -f "/home/wangweifan/data/anaconda3/etc/profile.d/conda.sh" ]; then
        . "/home/wangweifan/data/anaconda3/etc/profile.d/conda.sh"
    else
        export PATH="/home/wangweifan/data/anaconda3/bin:$PATH"
    fi
fi
unset __conda_setup
# <<< conda initialize <<<